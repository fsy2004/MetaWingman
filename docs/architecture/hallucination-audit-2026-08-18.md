# 幻觉与声称审计（2026-08-18）

按科研原则对本会话及近期产出的声称逐条核对：每个数字/结论的出处、验证方式、
修正动作。结论分三类：已证实、已修正、待精读。

## 1. 已证实为错误并修正的

| 声称 | 错误 | 证据 | 修正 |
|---|---|---|---|
| "glm-5.3 可用" | /models 列表里有，但当前 key 无权访问 | 实测调用返回 error 1220"您无权访问glm-5.3" | DSH cordis.patch.yml 已删 5.3；项目 config 标注"not authorized for this key" |
| （上一条的根因）把模型列表条目当可用模型 | 列表 ≠ 权限 | 同上 | 以后只写"实测可调用"的模型；未测的标 untested |

## 2. 已核实无误的（有 receipt/日志支撑）

| 声称 | 出处 |
|---|---|
| 12k 训练指标：section-role 0.9995；retrieval 候选集 0.962/0.933；全库 0.00452；loss 2.445 | 服务器 execution-receipt.json（实读） |
| 全库 TF-IDF 基线 MRR 0.220 / R@10 0.315 / P@1 0.170 | mw-baseline-v3.py 输出（实读） |
| R2-AI 数字：SR hosted 0.9385 vs verifier 1.0；RT hosted 0.465/0.93 vs verifier 0.9533/0.925 | 本地 validation-output/r2-ai-2026-08-18/scoring-results.json（本轮抽查，逐任务明细与汇总一致） |
| glm-5.2 可调用（推理模型） | 两次实测 completion 返回正常 |
| 五个 DSH 插件功能描述 | 各插件 README（读自本地 node_modules） |
| 12k 数据：11,875/12,000，125 源上不可得 | merge/fetch 日志（实读） |

## 3. 待精读原文才能采用的（本轮 web 搜索片段）

| 引用 | 现状 | 采用前动作 |
|---|---|---|
| Nature Medicine "LLM-assisted systematic review of LLMs in clinical medicine"（s41591-026-04229-5） | 来自搜索片段（含"random subset of 500 articles"），未读全文 | 精读方法部分后再引用细节 |
| Sakana AI Scientist 的"击败 55% 人类、单篇 $15" | 媒体转述 + 官方页，未核原文数字 | 引用时标"媒体转述"，写方案时只取定性思路 |
| Setlur ICLR'25 PRM、Ye ICLR-W'25 | mlnthology 有页面，之前会话已核 | 可直接引用 |

调研文档已加"引用核实状态"标注；**凡是搜索片段细节，方案映射只取定性思路
（人机混合验证、步骤级验证、新颖性过滤），不把片段数字写进任何声称**。

## 4. 结构性防幻觉措施（本次审计后生效）

1. 模型/API 类：只写"实测可调用"；列表条目一律标 untested/unauthorized。
2. 数字类：报告里的数字必须能指出 receipt/log 文件路径；做不到就不写。
3. 文献类：搜索片段 ≠ 事实；精读前只引用定性思路并标注来源类型。
4. 对外 README 维持"陌生人视角 + 声明边界"（readme-style-guide.md）。

## 5. 项目方向（基于已核实证据，不含未证实引用）

已核实的能力与缺口（证据见训练报告 §12-§14、R2-AI 结果文档）：

1. **两段式检索**（最实的下一步）：全库召回弱（0.0045 vs TF-IDF 0.220）而
   重排强（候选集 0.962）——TF-IDF/BM25 召回 + 12k 模型重排，直接消除最大的
   实测短板。
2. **GLM 双 provider 对比**：glm-5.2 已接入且实测可用；跑一遍 C0-C3 同配置
   对比 DeepSeek，得到"跨模型一致性"实证（也是跨模型能力的证据）。
3. **R6 步骤级评价 verifier**：RoB/GRADE 链步骤化 + 过程验证（Setlur 思路的
   定性版），落在"系统评价"这一用户指定的重点上。
4. **选题新颖性审计单**：候选选题输出"既有综述/注册比对 + 缺口 + 时间窗口"
   证据链（定性借鉴 AI Scientist 的新颖性过滤，不引用其数字）。
5. **VAL-2b 手册冻结**：复现评测（时间切分重建）的前置卡点，冻结后才可诚实
   跑 VAL-3。
