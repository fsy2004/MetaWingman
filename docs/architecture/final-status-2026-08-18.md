# MetaWingman 收尾状态（final status, 2026-08-18）

> 项目当前可交付状态的单一入口。所有数字均可回溯到各自的回执/结果文档；
> 每个"完成"后面是该证据的文档路径。rubric V2 训练与新代 VAL-2c 两项为
> 进行中项，其落定后本文件作终版更新。

## 1. 理念落地（五件套，全部可运行）

| 件 | 实现 | 证据 |
|---|---|---|
| Review Question Certificate | schema + 生成器 + 硬/软门 + 双法官盲评 | `generate_review_question_certificate.py` 冒烟；双 provider 盲评 4.0/4.4 |
| 十阶段苏格拉底清单 | 10 阶段 × 10 问（9 必答）+ 门禁 | `check_socratic_checklist.py` 全回归通过 |
| 步骤级验证器 | 规则版（10 步弃权/人工窗口）+ 6 域组件 | V3 规则一致性 0.8500；VAL-2c kappa 0.311 → 转向准则监督 |
| audit log + meta-update | JSONL + 人工窗口应用 + 提交回填 | 本会话 6 条闭环（glm-5.3、权重 bug、重建机制、BM25、GLM 整合、VAL-2c） |
| 双法官盲评 | 剥评分/门禁字段、Pearson 一致性 | 冒烟通过 |

## 2. 训练与组件（实测）

- 语料：109,028 例（section-role + evidence-retrieval 各 54,514；train 87,264 / dev 21,764），8.2GB、7 分片，全部确定性弱监督标签。
- 组件 1 section-role：eval macro-F1 0.9995。
- 组件 2 evidence-retrieval：候选集 MRR 0.962 / P@1 0.933；**开集检索定论 = BM25 单阶段（MRR 0.2649），训练重排器仅限 curated 候选集**（开放语料负贡献实测）。
- 组件 3 appraisal 6 域：规则一致性 0.8500；**规则清晰度 kappa 0.311 → 规则与准则判断不符 → 准则监督重标注 + rubric V2 训练（进行中）**。
- rubric V1（800 例薄数据基线）：macro-F1 0.193（预期内，域类训练例 0–3）。

## 3. 复现机制（实证）

- 首个重建案例（PLoS Medicine e1004082，R V̇O2peak 切片）：三重复锁定 + 评分通过（MD 2.865 vs 2.9、I² 92.67% vs 93%、k=16 精确、Egger 同侧）。
- ag-rdt living-update 家族：2021/2022 双工件许可核实 + 密封；操作语料冻结（PubMed 7,500 + 预印本 5,000 → 合并 12,498）；筛选锚点冻结；确定性链演练完成并量化边界（词项锚点 = 42% 保留的分诊器；摘要级提取覆盖率 17%/15%/42%）。
- VAL-3 首个 AI 筛选：黄金召回 0.765（弃权为主损失 → 全文阶段方向）。

## 4. 评测对标

- MetaSyn（arXiv:2606.17041v6）任务映射 15 项：covered 6 / partial 7 / gap 2；数据集卡许可已核实；适配器设计已冻结（五条前置条件）。
- 跨 provider：GLM C3 vs DeepSeek C3-R2 同盲集（F1 0.9003 vs 0.9385；检索选择准确率 0.96 vs 0.93；kappa 0.872, 95% CI 0.848–0.896）。

## 5. 验证阶梯状态

VAL-1 ✅（首晋升）| VAL-2a ✅ | VAL-2b1 ✅ | VAL-2b2 ⏳（首案例手册 ✅，其余家族开放）| VAL-2c ✅评分（0.311 → 转向）| VAL-3 ⏳（首 AI 筛选 ✅，全配置 pilot 开放）。

## 6. 进行中（收齐即自动完成）

1. train 切分 7,932 条准则评分（4 shard）+ dev 剩余 1,174 条 → 最终准则训练集；
2. rubric V2 训练（服务器）→ 回执 + 哈希；
3. 新代 VAL-2c 冻结 + 评分 → 准则一致性报告；
4. 本文件终版 + 白皮书/路线图/目标审计终版 + 目标完成判定。

## 7. 诚实边界（不变）

所有"一致性"均为对冻结标签/准则判断/已发表参考的**一致性测量**，不是科学验证、不是人类对比、不是临床有效性。每项文档均按此边界措辞。
